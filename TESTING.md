# 测试与实机验收

## 0.1.13 / Windows preview.3 安全与可靠性

- 新增合成 SIM 切换／未知身份、首次绑定期限、旧账本备份不导入、旧库迁移、未来 schema 拒绝打开、损坏库暂停和原文件保留测试。
- 新增短信槽位复用、设备变化、部分清理、AT 查询失败／删除失败验证；Wi-Fi／数据 OFF 转发链仍使用隔离假设备与假 Messages。
- macOS 使用合成正文验证标准输入中文／Emoji 往返，并检查测试进程参数；编译完整脚本时以无效操作在访问 Messages 前退出，不发送任何真实消息。
- Windows 测试通知合并／注册失败、独立探测超时／取消／回收、计量锁独立和开启中达到保护线。打包自检新增无联网探测子进程测试与系统通知注册／注销。
- 构建、测试计数与产物检查结果见本版发布记录；这不是 Windows 真机或真实短信送达验收。未开启数据、未发送查询或 iMessage、未删除真实模块短信。

## macOS 0.1.10 界面与套餐上限（2026-09-26）

- 238 项测试通过，Ruff、strict mypy、敏感信息扫描通过；新增默认套餐保护、旧 GB 授权不扩大、已授权套餐保持三项迁移回归。
- 真实打包 `.app` 的八页深浅色渲染、菜单面板、所有数据状态、侧边栏双向联动、1000×700／1280×820 窗口布局检查通过。保留未保存目标、缺模块禁用及诊断模式测试。
- `artifacts/ui-smoke` 是隔离界面测试产物，侧栏明确标记模拟数据，不接触 USB、钥匙串、短信或网络；其中套餐数字不是真实账单。
- 新版普通启动后通过 macOS 辅助功能树与真实窗口截图检查：侧栏可进入设置，4G 上限显示运营商套餐总量，规则为 80% 确认／98% 停止。模块可识别，界面数据状态为关闭；没有点击开启数据、查询或消息测试按钮。
- arm64 `.app` 通过签名、Info.plist、真实打包 UI 验证，DMG 创建与只读挂载检查通过。为 ad-hoc 签名，未公证；此轮不宣称重新完成实际断网接管或 iMessage 送达测试。

## Windows 0.2.0-preview.1（2026-09-26）

- 新增 41 项标准库回归测试；与原 macOS 套件合计 235 项通过，Ruff 和 strict mypy 通过。原 macOS 覆盖门槛保持：整体 92%、原核心 95%；这些百分比不包含 Windows 系统适配层，不宣称 Windows 整体覆盖率达到该值。
- 首次 Windows CI 配置有 YAML 参数冒号转义问题，修复后 [Windows run 36228410419](https://github.com/pameda/4G-Bridge/actions/runs/36228410419) 通过：当时的 34 项 Windows 测试、Win32 结构尺寸、系统网卡枚举、2 个接口计数读取、960×730 六页 Tk/ttk 启动检查。
- 早期源码 smoke 未携带打包图标，托盘验证记录为 false；不能把窗口启动当成托盘已验证。后续正式打包检查结果见下方。
- 后续新增探测接口绑定、查询确认过期／换卡取消、并发查询拒绝、睡眠撤销授权、开启失败回滚测试；不得用逻辑 mock 测试代替硬件验收。
- 最终源码 `454f20b` 的 [Windows run 36228792202](https://github.com/pameda/4G-Bridge/actions/runs/36228792202) 再次通过：41 项 Windows 回归、只读系统接口检查和六页源码 UI 启动；第三方依赖安装、EXE 打包与安装验收步骤按未授权状态跳过。
- 用户批准第三方打包工具后，修正 Python Windows 安装中缺失独立 Tcl/Tk 许可文件的问题：随包附上官方 9.0.4 标签许可，固定 Python 3.14.7，未跳过许可要求。
- 发布构建 `833a28a7fbb794c55653f5e9fd730ac777c50bcd` 的 [Windows run 36231330207](https://github.com/pameda/4G-Bridge/actions/runs/36231330207) 全部成功：41 项 Windows 测试、哈希锁定依赖安装、EXE／NSIS 安装器构建、打包后的系统接口检查、六页 UI 与托盘启动、静默安装、安装后的 UI／托盘启动、卸载后主程序不存在检查。
- 环境为 Windows Server 2022 x64；打包与安装后报告均为 `pages: 6`、`window: [960, 730]`、`tray: true`、`mutations: 0`。原生报告确认 DCB 28 字节、接口结构 1352 字节、两个系统接口计数可读取。这是启动检查，不是交互式视觉验收或完整功能实机测试。
- 下载产物已核验 PE x86-64、许可文件和 SHA-256；安装器 `a09dd3d31ac61692bf3ced550b74c2db8c68cbb3b52044e666d1fe6330c721bc`，便携 ZIP `a7806ae3dd6b0544884f77938b4d112a768d971fc33c1116490283f7b0a00286`。本机最终 235 项测试、Ruff、mypy 和敏感信息扫描仍通过。
- 没有安装新构建依赖到用户 Mac，没有发送查询短信，没有启停用户网络，没有运行 iMessage。Windows runner 无 QDC507，SIM／AT／上网／VPN／睡眠唤醒硬件测试均为 **未执行**。


## 0.1.9 应用网络、运营商比例保护与中文日志

- 194 项自动化测试通过；整体覆盖率 92%，原核心 95%，新增查询／计数／套餐保护／事件模块合计 96%。Ruff、strict mypy 通过。
- 普通启动发现菜单提前读取尚未初始化的保护模块，已增加启动占位状态与专门回归；不能以界面夹具测试代替真实启动。
- 八页原生界面与可滚动菜单在浅色／深色 smoke 通过；测试截图包含明确的夹具，不作为真实套餐证据。
- 实机应用网络显示原生进程名称、图标、实时字节速率；采集无远端地址，不进行应用分流，不标注推测的 Wi-Fi／4G 出口。
- 实机日志确认仅一次电信查询操作，约 3 秒收到回复；后续只读复用原有模块短信，没有再发送、没有主动删除。逐项通用流量／结转解析验证成功，真实套餐金额、号码、正文不写入仓库。
- 原自动接管保存值为关闭，固定额度为零；旧 VPN 完全暂缓及长间隔也会阻止接管。新增明确链路断开快路径，VPN 默认隧道保留、模块 scoped 路由验证；真实 Wi-Fi 断开后的完整接管仍需最终应用验收。
- 当前 Wi-Fi 实测：旧探测失败，系统 DNS 返回代理虚拟地址；新请求级解析回退后 Wi-Fi 检测成功，未改系统 DNS 或 VPN。所有探测、包括固定域名的 DNS 查询，均绑定 Wi-Fi；没有把探测成功等同于 VPN 切换成功。
- 套餐保护覆盖 79／80／97／98／100% 边界、双向新增、计数归零、重启、80% 本次连接同意、98% 持久锁定、过期／新月与独立守卫关闭。未通过实际耗尽 SIM 套餐来测试阈值。
- 登录启动使用 SMAppService；测试假服务及系统状态读取，不擅自注册、不退出用户登录验证。
- 中文日志固定事件白名单，500 条内存上限，筛选／清空／拒绝任意私密文本已测试；无日志文件导出默认行为。
- 本次是 ad-hoc 未公证构建；更新后 Messages 转发可能需用户重新授权钥匙串。没有额外发送固定测试 iMessage。
- 最终普通启动已成功显示模块与八页窗口；真实运营商剩余量已显示，用户指定的 80%/98% 保护和自动接管已通过界面启用。Wi-Fi 可用期间系统 QDC507 服务仍为 Disabled，默认路由仍为原 VPN 的 utun4。已请求最终断开／恢复测试许可，未在未获准前更改 Wi-Fi。
- 更新后的转发等待钥匙串授权，已从应用发起；运营商回复仍在模块中，不能将已解析的套餐数据当作 iMessage 已送达。

## 0.1.8 短信默认自动转发

- 140 项自动化测试通过，Ruff format/check、strict mypy、打包应用 UI smoke 通过。
- 新配置和缺失的转发字段默认为开启；显式关闭跨重启保留，损坏配置及非法布尔值安全关闭。
- 诊断模式仍不轮询 USB／短信；明确退出后恢复已保存的转发开关，不再强制关闭。数据保护及 Wi-Fi-only 转发回归保持通过。
- 本地目标前置检查不调用 Messages：缺目标、格式无效、钥匙串不可读时不调用短信轮询，不创建或消耗重试任务；配置就绪时才进入原有去重／发送／清理流程。
- 本机最终正常进程与保存设置均确认转发开启，4G 数据界面显示关闭。当前显示等待钥匙串授权，已从应用发起系统授权请求；没有额外发送固定测试消息，不把开关开启当成真实送达证据。
- arm64、ad-hoc 签名、DMG 只读挂载检查通过。未公证，更新后仍可能需要系统重新授权读取目标。

## 0.1.7 模块控制恢复与实机联网

- 根因确认：旧进程仍带 `--relay-diagnostics`，`rescan()` 完全跳过 USB；系统却能识别 `2C7C:0125`。这不是重插失败，而是测试模式未退出且界面误报未发现设备。
- 132 项自动化测试通过，Ruff、mypy、真实打包五页深浅色 UI smoke 通过；覆盖恢复幂等、保存失败不恢复、先保存短信关闭再检测、诊断动作不越权及明确提示。
- 真实 USB 只读查询：SIM READY、CEREG 0,1、CSQ 24（约 −65 dBm），ECM 动态发现 en11。
- 通过最终应用点击“恢复模块控制”，正常检测到 QDC507、中国电信、FDD LTE；恢复前安全保存短信转发关闭，未读取或删除模块短信。
- 使用此前允许的小流量测试：应用实际完成数据 ON，并取得模块 IPv4／网关；使用 en11 绑定 HTTPS HEAD 访问公开站点，HTTP 200、TLS 验证通过。HEAD 不下载正文，但连接和握手仍产生少量流量。
- Apple 域名探测先超时，发现现有代理 DNS 返回 198.18 虚拟地址；直接 1.1.1.1 的 HTTPS 也超时。公开 DNS 查询取得真实地址后，国内站点绑定网卡请求成功。不能把单一站点失败当成模块整体不可用。
- 测试结束从应用关闭数据，系统服务 Disabled；默认接口保持既有 VPN 的 utun4。没有改 DNS、VPN，不宣称测试了 Wi-Fi 断开后的自动接管或 VPN 底层出口迁移。
- 短信自动转发暂时关闭，避免处理积存短信；需要用户明确恢复。
- 最终只保留普通 `--settings` 启动进程，窗口重新确认 QDC507／中国电信／FDD LTE、en11 与可用的开启按钮；不再遗留诊断参数。
- 本地 arm64、ad-hoc 签名、DMG 只读挂载验证通过；仍未公证。

## 0.1.6 蓝色菜单栏徽标

- 127 项自动化测试通过，整体覆盖率约 92%，核心覆盖率 96%；Ruff format/check、strict mypy 全部通过。
- 检查所有数据状态的徽标文字对比度 ≥ 4.5:1，断开与暂停有不同形状，警告不依赖颜色传达。
- 实际打包应用执行五页深浅色与菜单栏 UI smoke，验证非模板彩色图像、固定宽度、辅助功能名称、可点击状态及禁用图像缓存。
- 使用生产渲染器输出 1×／2×、浅色／深色／选中背景下的六种状态，像素检查防止黑色或空白图标，人工检查图像；夹具不代表真实设备状态。
- `script/preview_status_badge.py` 替代旧符号预览脚本，输出 `artifacts/menu-badge-preview.png`。
- arm64、嵌套 ad-hoc 签名、Info.plist、DMG 只读挂载卸载与 SHA-256 校验通过；未公证。
- 新版以 `--relay-diagnostics` 启动，实际设置窗口可用；随后收起窗口保留菜单栏。没有开启 4G、读取／删除模块短信或发送 iMessage，没有恢复后台队列。
- 本版仅改展示，不把 UI 回归作为蜂窝上网、自动接管或短信送达的实机验收。

## 0.1.5 Wi-Fi 转发与菜单栏

- 116 项测试通过，整体覆盖率 91.76%，核心覆盖率 96%；真实打包 UI smoke 的五页深浅色、固定菜单栏宽度／模板图标／可点击状态通过。
- Wi-Fi-only 回归使用真实 PDU 解码、组装、去重、格式化和状态存储，仅 USB 与 Messages 边界替换为测试对象。数据 OFF 时仍调用 Messages；重复读取不重发。ECM 异常不会关闭 AT 通道。
- `script/preview_status_symbols.py` 用实际系统符号离屏绘制状态预览，不读取用户屏幕、短信或网络。
- 经用户明确授权后执行一次固定 iMessage 测试：用户完成系统钥匙串授权，应用连接检查通过，Messages 接受了唯一一条发送请求。未重复发送。
- 发送前后 QDC507 网络服务均为 Disabled，Wi-Fi 有地址，默认路由为既有 VPN 的 utun4。保留 VPN，不把这项观测当作证明 VPN 的物理出口必然为 Wi-Fi。
- 未读取、转发或删除模块历史短信。iPhone 送达结果等待用户确认；实际 QDC507 新短信接收到 iPhone 的完整链路仍未验收。
- 诊断模式保留原有设置，关闭自动工作，仅提供显式检查／测试入口。真正恢复后台运行需退出诊断模式后正常启动。

## 0.1.4 自动接管与 iMessage 授权修复

- 104 项自动化测试通过，整体覆盖率 91.63%，核心覆盖率 96%。覆盖三次失败／两次恢复防抖、VPN 和其他默认网络保护、限额跨重启／跨月／重编号保持锁定、手动额度、损坏计量不重建、设置丢失不绕过锁定、计量失败安全关闭、最小服务顺序调整及过期异步回调拦截。
- 构建后的 `.app` 执行 `--bridge-check`，约 0.14 秒返回 `keychain_unavailable`、`sent:false`。修复前该路径等待系统钥匙串授权而挂起。除现代 query 标记外，兼容 login keychain 的进程级禁止交互策略，并串行恢复原始标记。
- 最终打包五页深浅色 UI smoke 通过，设置页展示自动接管、GB 上限、周期和手动追加额度；这些截图使用无副作用测试数据，不是真实流量。
- 新版本没有正常启动短信接收／自动转发，没有改变用户真实目标或额度，没有发送 iMessage、读取／删除模块短信、开启 SIM 数据或改变网络服务顺序。
- **尚未实测**：真实 Wi-Fi 断网／恢复自动切换、限额触顶关闭、VPN 底层出口迁移、睡眠唤醒与 iMessage 端到端发送。用户尚未填写额度，自动接管保持未启用；不把模拟通过当作真实联网成功。
- iMessage 需要用户在应用中点击“授权读取目标”，按系统提示完成授权；保存含国家区号的目标后先检查，再由用户确认发送测试。AppleScript 接受请求不等于送达。

## 0.1.3 iMessage 排查

- 本机 Messages.sdef 与 AppleScript 编译通过；只读查询确认 iMessage 账户 enabled/connected。
- 检查保存目标格式与应用自有队列状态，不读取 Messages chat.db、聊天内容或账号凭据，不记录目标标识。
- 新增 `check` 路径在 `send` 之前返回；服务检查通过不代表联系人可达或已送达。
- 78 项 pytest 通过，整体覆盖率 90.86%，核心覆盖率 95%；包含超时不重发、清理异常保留已接受状态、钥匙串错误与国际号码格式验证。
- 排查时正常退出自动转发应用，保持原始设置和历史队列，不重新发送历史已处理项，不删除模块短信。真实发送仍待用户确认，不能把本次自动化结果视为端到端恢复。

## 自动化门禁

运行：

```bash
./script/test.sh
```

门禁依次执行 Ruff format/check、strict mypy 与 pytest/coverage。0.1.2 本机结果为 70 passed，整体覆盖率 89.88%，核心业务模块覆盖率 95%；门槛分别为 80% 与 90%。AppKit 生命周期胶水、UI 和 Keychain 系统绑定不计入核心覆盖率，通过真实打包 UI 检查验证。

0.1.2 新增无硬件副作用的五页深浅色、快速面板截图检查，以及数据状态、缺设备、主题切换、未保存目标保护、真实采样展示／断开清空断言。视觉开发采用内存夹具，不代表实机流量；测试不会发送短信、iMessage 或开启 SIM 数据。0.1.1 的联网实测记录保留，不能当作本版重新执行了联网测试。

GitHub Actions 的既有 `v0.1.0`、`v0.1.1` 发布工作流已通过；tag 工作流在官方 arm64 macOS runner 上重新安装锁定依赖、重跑门禁、生成并发布 DMG。各次运行的最终状态以 GitHub Actions 为准。

### 0.1.2 本机界面验收

- 构建产物直接执行 UI smoke：五页 × 深浅色、原生 NSPopover 的实际打开／关闭与截图均通过，无布局冲突或 traceback。
- 正常启动最终 `.app` 并查看实际窗口：模块 QDC507、中国电信、FDD LTE、当前接口与信号正常显示；数据 OFF，转发未开启。
- 仅截取应用自身窗口核对工具栏、分组卡片、数字和布局，没有收集其他应用画面。
- DMG 只读挂载与卸载、arm64、ad-hoc 签名、Info.plist、SHA-256 检查通过。校验文件使用相对文件名，便于下载后直接校验。
- 本轮没有重新执行收费上网探测、SMS 收发／删除或 iMessage 发送。未公证，不能保证首次安装不出现 Gatekeeper 提示。

覆盖内容包括 AT 分片/URC/prompt/timeout、Unicode/Emoji PDU、8/16-bit multipart、去重、重试、cleanup、SQLite 损坏隔离、AppleScript argv 注入防护、USB endpoint 变化、ECM 重编号、数据确认门、counter reset、日/月流量、设置权限与日志脱敏。

## 构建验证

`./script/build_and_run.sh --build-only` 会先运行全部门禁，再生成 arm64 app、逐个签名 Mach-O、验证 bundle 签名、Info.plist、LSUIElement、bundle identifier 和架构。

`./script/build_and_run.sh --package` 额外生成 DMG，挂载只读验证后输出 SHA-256。

旧版仅检查进程存在，曾漏掉 py2app 启动错误。0.1.1 构建增加实际二进制 `--ui-smoke`：不访问 USB、Keychain 或短信，构造真实菜单及四个设置页，渲染深浅色截图，要求明确完成标记，且不允许 Python traceback 或布局冲突。另执行正常应用启动和界面检查。

## 0.1.1 上网故障与界面验收

- 发现并修复 `(*)` 禁用服务解析错误：它曾把 QDC507 错绑到上一块网卡。
- 删除通用 USB Ethernet 猜测匹配；回归覆盖 en9 普通网卡与 en11 模块并存。
- 禁止普通 AT 连接发送 USB SET_CONFIGURATION，且不探测 CDC 数据接口。
- 修复 DHCP 实际输出 `router (ip_mult)` 的网关解析；无地址/网关时不报成功。
- 发现同时运行的 DJI4GMenuBar 与 AT 辅助进程，已正常退出以隔离测试；应用新增冲突提示。
- 本机 macOS/QDC507 组合中，即使无控制器占用，networksetup 关闭再开启仍可复现 ECM inactive；增加停机等待也未恢复。模块重启后可正常取得地址。
- 新增仅在用户确认开启后的有界恢复，实测从失活状态恢复为 ON，获得模块私网地址和网关，经 en11 完成 HTTPS 200，返回正文约 1 KB，最后验证 OFF。
- 首个 1.1.1.1 HTTP 探测超时；不能把单一探测点失败当成整个 4G 不可用。随后通过绑定 en11、固定服务器地址并校验 TLS 的 HTTPS 请求验证成功。
- 默认路由仍为现有 VPN 的 utun4，Wi-Fi 服务仍第一；这不等于已测试关闭 Wi-Fi 后的 VPN 底层出口切换，该项仍待验收。
- 新界面使用原生 NSTabViewController 工具栏、系统开关、语义色和自动布局；检查四个页面、深浅色和完整原生窗口截图，未见裁切。
- 未发送 iMessage、未读取短信正文、未删除短信。睡眠/唤醒、完整断网切换和短信端到端验收仍未执行。

## 2026-09-25 QDC507 实机结果

- USB：`2C7C:0125`，制造商 `BAIWANG`，产品 `EG25G-QDC507`。
- 固件标识：`QDC507GLEFM21`；USB 网络模式为 ECM (`usbnet=1`)。
- SIM：`READY`；运营商 `CHN-CT`；注册状态 `registered_home`；RAT `FDD LTE`。
- 信号：`CSQ 20`，换算约 `-73 dBm`。
- 当前 ECM 接口动态识别为 `en11`，网络服务为 `EG25G-QDC507 2`；同时存在普通 USB 网卡 `en9`，已验证不会再被误判为 QDC507。
- 应用启动后 QDC507 网络服务处于禁用状态；Wi-Fi 保持服务顺序第一，实际公网路由保持现有 VPN 的 `utun4`，没有修改 DNS、静态路由或其他服务相对顺序。
- 修复并验证两个真实产物启动问题：PyObjC 内部方法 selector 误解析、错误的睡眠通知常量。修复后的 ad-hoc arm64 `.app` 连续运行超过十分钟，无 traceback 或启动器弹窗。
- 短信转发设置保持关闭；未读取短信正文、未发送 iMessage、未删除模块短信，也未执行 4G 数据 ON 测试。

## 实机测试顺序

1. 不插模块启动，确认无崩溃且数据 OFF。
2. 插入模块，只读检查 VID/PID、AT endpoint、SIM、运营商、注册和信号。
3. 拔出并重插，确认旧 session 释放，endpoint 和 ECM 接口重新发现。
4. 分别测试未插 SIM、未注册、无信号和 AT timeout。
5. 保持 Wi-Fi 与 VPN，确认 Wi-Fi 始终是默认接口，VPN 与其他服务相对顺序不变。
6. 睡眠/唤醒后确认数据 OFF。
7. 取得明确确认后测试数据 ON/OFF；记录前后 network service 与 `CGATT`。
8. 取得明确确认后发送固定 iMessage 测试文本。
9. 取得明确确认后接收专用测试短信，验证成功后才删除模块短信。

任何收费、发送或删除动作都不是自动化测试的一部分。
# 0.1.11 导航修复验证（2026-09-26）

- 238 项自动化测试通过；格式、Ruff、mypy 通过。
- 已构建的 arm64 App 完成八个页面深浅色、尺寸变化验证；新增全部侧栏文字／图标／单元格命中检查，统一由 NSTableView 接收；单选索引与页面一致。
- 构建阶段采用 AppKit 实际布局的命中测试和选择回调验证；用户授权安装后，已在 `/Applications/4G Bridge.app` 实际点击“本机流量”文字，确认页面切换和单选高亮一致。
- Wi-Fi 断开立即请求接管、恢复后连续两次检测成功回切、VPN 保留和额度阻止等现有模拟测试通过；未实际断开用户 Wi-Fi。实际接管还需要模块建立连接，并非零延迟。
- 当前自动接管及套餐保护设置均已开启；本次未更改网络策略或额度状态。
- 使用 `./script/build_and_run.sh --package-isolated` 构建时保留旧版连接；随后经用户授权安装到 `/Applications/4G Bridge.app` 并启动，保留用户设置。安装前确认旧进程已退出、模块服务关闭；启动后原有自动接管授权继续生效。
- 独立 App 位于 `artifacts/staged-dist/4G Bridge.app`；DMG 为 `artifacts/4G-Bridge-0.1.11-arm64.dmg`，完成签名完整性和挂载验证。仍为 ad-hoc、未公证。
- 本版只修复导航；此前安全审查发现项尚未修复，不应据此视为安全验收完成。
# 0.1.12 百分比与预警配色验证（2026-09-26）

- 254 项自动化测试通过；新增 16 项显示边界测试，包括无数据、非有限值、0/60/75/80/98/100% 及阈值前的小数。
- 设置页使用大号已用百分比，当前区间同时以文字、着色边框和背景标记；圆环与百分比统一使用系统蓝/橙/红语义色，不仅依靠颜色传达状态。
- 隔离 AppKit 验证全部状态在深浅色下的百分比同步、颜色同步与当前区间唯一性，并生成设置页、套餐页截图；无真实套餐或网络修改。
- 75% 为视觉提前预警，不改变 80% 确认和 98% 停用的数据策略。过期及计量不可用状态仍以保护状态文字展示，不能仅凭圆环蓝色判断可联网。
- 0.1.12 为独立构建，未替换正在运行的 0.1.11；仍为 ad-hoc 未公证安装包，安全审查待修项不因本次视觉调整而关闭。
