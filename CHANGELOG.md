# Changelog

## 0.1.13 / Windows 0.2.0-preview.3 — 2026-09-26

- Mac：套餐按 SIM 指纹隔离；安全迁移保留旧账本，但要求首次重新查询套餐。
- Mac：逐段身份／PDU 核验后才删除短信；旧清理凭据缺失时暂停，不误删或重发。
- Mac：iMessage 目标与正文改走标准输入；数据库损坏／版本过新不再自动重建空去重库。
- Windows：网络事件唤醒、探测进程 deadline／取消／回收、计量与网络控制分锁、中文故障建议。
- 不新增权限或依赖；仍无 Windows 实机模块验证，不代表已完成端到端联网或 iMessage 送达验收。

## 0.1.12 — 2026-09-26

- 设置页突出套餐估算百分比和当前区间，使用文字、边框和颜色共同提示。
- 所有套餐圆环统一为低于 60% 蓝色、60% 至 75% 橙色、75% 起红色提前预警；80% 确认、98% 停用策略保持不变。
- 百分比保留一位小数且不跨阈值进位；补充未知数据、边界、深浅色区间展示验证。

## 0.1.11 — 2026-09-26

- 修复左侧导航文字、图标拦截点击：整行交给原生列表处理，支持未激活窗口的首次点击。
- 使用原生单选高亮，避免自绘背景残留；补充全部导航项的文字／图标命中测试与单选同步验证。
- 本次仅调整导航交互，不改变网络、套餐保护或短信逻辑；安全审查发现项仍待另行修复。

## 0.1.10 — 2026-09-26

- 重做 Mac 窗口导航：原生材质侧边栏、八个清晰入口、可缩放窗口与可滚动页面，移除拥挤的顶部八按钮工具栏。
- 双栏连接总览突出蜂窝状态与套餐环形图；统一系统底色、细边框、间距、字体层级与等宽数字；完整支持深浅色。
- 菜单栏改用 SF Symbols 绘制状态图形，保留蓝色徽标；快速面板紧凑重排，显示套餐、本机流量、转发与应用网络。
- 按用户要求将 Mac 的 4G 上限统一为运营商套餐总量，移除手动 GB 输入和追加额度入口。保留 80% 本次确认／98% 关闭、未知或过期数据保护；旧 GB 授权不自动扩大到套餐，既有套餐授权保持。
- 新增迁移回归与真实打包界面的侧边栏联动、窗口缩放检查。没有改变短信发送、去重、清理与钥匙串实现；Windows 版未修改。

## 0.1.9 — 2026-09-26

- 新增原生应用网络进程列表：实时上下行、内存观测累计、暂停与菜单前三项，不采集地址或流量内容。
- 新增 SMAppService 登录启动开关与系统状态反馈，不默认注册。
- 新增确认式运营商短信查询，发送结果不确定时不重试；保留未成功转发的回复。
- 新增运营商套餐环形图：明确总量／已用量才展示百分比，支持缺测、90% 警告和深浅色。
- 新增中文运行日志，固定安全事件白名单、内存 500 条上限、警告筛选、复制及清空。
- 新增运营商 80% 确认／98% 关闭保护，数字元数据持久化、查询过期关闭、无后台收费短信。
- Wi-Fi 明确断开直接请求接管；修复 VPN 存在时完全不接管及代理虚拟 DNS 导致正常 Wi-Fi 被误判的问题，不修改隧道或系统 DNS。
- 保留 iMessage 去重规则，运营商查询回复在转发不可用时仍可只读解析，不删除。

## 0.1.8 — 2026-09-26

- 短信转发默认开启，保留用户手动关闭的选择；退出诊断模式恢复保存的开关，不再强制关闭。
- 未配置目标、目标无效或钥匙串未授权时，暂停短信轮询等待配置，不提前耗尽重试次数。
- 损坏设置安全关闭；不改变 4G 数据、Wi-Fi 优先、流量上限和去重／清理规则。
- 增加默认值、迁移、手动关闭持久化、诊断恢复和目标前置检查回归。

## 0.1.7 — 2026-09-26

- 修复短信测试模式遗留导致模块始终“未发现”、重插和重新检测无效的交互缺陷。
- 窗口标题、总览、设备页和菜单明确标识“模块检测已暂停”；增加恢复模块控制入口，不把未执行检测当作设备缺失。
- 恢复前保存短信转发关闭状态，防止意外处理积存短信；恢复检测、流量保护和原有自动接管策略，数据先进入 OFF。
- 增加恢复、幂等、防越过诊断隔离、保存失败安全停止及诊断界面测试。

## 0.1.6 — 2026-09-26

- 菜单栏改为一体化蓝色连接徽标，移除黑色模板图标与分离的文字；固定 50 pt 点击区域，不随信号变化跳动。
- 浅蓝待命、蓝底白字联网、琥珀警告；断开、暂停、过渡、真实信号档位使用不同形状并保留辅助功能说明。
- 原生矢量绘制，支持深浅色菜单栏与 Retina；不缓存跨主题颜色，预览和应用共用同一渲染器。
- 新增全部状态的文字对比度检查与 1×／2× 原生渲染验证；不改变数据接管、流量限制或短信转发策略。

## 0.1.5 — 2026-09-26

- 重做菜单栏：固定紧凑宽度、14 pt 系统蜂窝信号符号、4G 字样和真实信号档位，避免尺寸跳动；离线／过渡／警告状态使用清晰单色符号。
- 明确并验证 Wi-Fi 与 4G 数据关闭状态下的 SMS → iMessage 流程：转发不依赖蜂窝数据、自动接管或流量额度。
- 短信轮询优先于慢速状态查询；ECM/DHCP 查询异常不再销毁 AT 短信会话。
- 增加安全转发诊断界面，可单独授权、检查或手动测试，不读取／转发／删除模块短信，不启动自动接管。
- 新增实际解码／组装／去重流程的 Wi-Fi-only 回归、诊断无副作用测试，以及菜单栏深浅色和模板图标检查。

## 0.1.4 — 2026-09-26

- 增加授权式 Wi-Fi 故障自动接管：三次失败后接管、两次恢复后关闭，仅移动 QDC507 网络服务，VPN／其他默认网络存在时暂缓。
- 增加 GB 限额、每份额度／自然月计量与持久化到顶锁定；重启、重插、跨月和更改设置不解锁，手动确认追加额度后方可继续。
- 独立限额保护线程不等待短信或 Messages；计量异常关闭，自动连接失败不循环重启模块。
- 初始限额未设置、自动接管未授权，未产生新的 SIM 流量；实机切换验收仍待用户配置与测试窗口。
- 最终应用诊断复现了 Keychain 授权等待：更新 ad-hoc 构建后，读取目标可能挂起。
- 后台和界面自动读取改用不允许交互的查询，未授权立即返回明确错误，不触发隐藏授权等待。
- 增加“授权读取目标”操作，仅在用户点击后通过后台线程发起系统钥匙串授权，不发送消息。
- 增加未授权、条目缺失与显式授权测试。未擅自修改保存目标或恢复历史转发。

## 0.1.3 — 2026-09-26

- iMessage 增加不发送消息的连接检查，使用本机 Messages dictionary 的 account/participant 接口。
- 目标要求明确国家区号或有效邮箱；不自动修改现有目标，防止歧义。未保存的编辑不会误用旧目标发送。
- 检查与测试移到后台，界面显示进度、具体错误与投递限制；捕获钥匙串和脚本启动错误。
- 发送超时或进入发送阶段后的错误标记为 delivery_unknown，保留模块短信，不自动重复发送。
- 接受发送请求后先持久化 cleanup_pending 再删除；清理异常不导致重复转发，队列展示 FAILED 状态。
- 新增目标校验、只读检查、超时防重复和删除异常测试。未执行真实消息发送验收，不宣称投递问题已解决。

## 0.1.2 — 2026-09-25

- 新增菜单栏原生快速面板，左键快速控制、右键保留完整菜单。
- 重组为总览、流量、短信转发、设备、设置五页：分组卡片、清晰状态层级、等宽流量数字和 SF Symbols。
- 新增真实网卡速度趋势，独立显示本次／今日／本月流量；明确本机计数与运营商套餐余额的区别。
- 新增跟随系统／浅色／深色外观并安全持久化；兼容旧设置，不改数据默认关闭行为。
- 测试发送、重发和确认清理增加二次确认；保留未保存的目标编辑，断开网卡后清除过时速度。
- 共享无副作用的展示模型，增加格式化、趋势去重／断档、状态文案和设置迁移测试。
- 不新增电话、固件刷写、自动流量查询短信、应用级监控或 Messages 数据库访问。

## 0.1.1 — 2026-09-25

- 重做设置窗口：原生工具栏、连接状态分组、系统开关、对齐的信息行和深浅色界面；菜单精简，技术详情移到子菜单。
- 修复 macOS 禁用服务的 `(*)` 解析错误，避免错误控制上一块网卡；删除通用 USB Ethernet 猜测匹配。
- 避免 AT 会话重设整个 USB 设备配置，排除 CDC ECM 数据接口；避免重复执行蜂窝附着。
- 支持真实 DHCP 的 `router (ip_mult)` 格式，开启时等待 IP/网关，失败回退关闭并展示原因。
- 数据开关移至后台，界面显示连接进度；仅移动 QDC507 服务以保留其他网络服务相对顺序。
- 针对实机 ECM 关闭后无法恢复的问题，增加用户确认开启流程内的一次有界模块重启；轮询、启动和重连不会自动开启。
- 提示退出同时运行的 DJI 控制器。构建必须执行真实打包 UI 的四页深浅色检查，避免仅凭进程存在误报启动成功。

## 0.1.0 — 2026-09-25

- 建立 clean-room Python/PyObjC 菜单栏应用。
- 添加 QDC507 双 VID/PID、动态 bulk endpoint 与单 AT reader worker。
- 添加 SIM/LTE/信号快照和动态 ECM 网络发现。
- 添加 GSM 7-bit、UCS-2/UTF-16BE、Emoji 与 multipart SMS 管线。
- 添加最小 SQLite relay 状态、去重、退避、delivery unknown 与 cleanup pending。
- 添加 Messages AppleScript argv bridge 与明确错误映射。
- 添加安全数据 OFF/ON 状态机与接口 counter 流量统计。
- 添加原生中文菜单栏和设置窗口、Keychain 目标存储。
- 添加 arm64 py2app、ad-hoc 签名、DMG 和 GitHub Actions 工作流。
- 添加 49 项自动化测试，整体覆盖率 89.00%，核心业务模块覆盖率 95%。
- 修正注册查询响应与同名 URC 的分流，补充 ATI、QCFG、QCCID、CNUM 与三级注册状态读取。
- 设置页增加模块网络详情和 `delivery_unknown` 人工重试/确认入口。
- py2app 明确排除未使用的 Tcl/Tk，缩小 App 并提高远端签名可复现性。
- Wi‑Fi 与 4G 共存时保持 Wi‑Fi 默认路由，验证失败自动回滚关闭 4G。
- 修复打包应用中 PyObjC 将菜单内部方法误解析为 selector 导致的启动失败。
- 使用正确的 macOS 睡眠通知并通过真实 `.app` 启动验证。
- 优先匹配 Baiwang/QDC507/EC25/EG25G，避免把普通 USB 网卡误识别为模块 ECM 接口。
- 完成 QDC507 `2C7C:0125` 实机只读验收；自动化测试增至 50 项。
# Windows 0.2.0-preview.1 — 2026-09-26（预览，未完成实机验收）

- 按用户要求新增 Windows x64 版本；不包含 iMessage、Apple 账号或短信转发。macOS 版本号与行为保持独立。
- 共享 AT parser、PDU 组装、运营商解析和 80%／98% 套餐保护；Windows 使用系统 COM、网卡枚举、64 位计数、托盘和用户登录项。
- 新增中文六页界面、环形图、日志、IPv4 TCP 应用统计、SIM 隔离账本和可恢复的模块优先级修改。
- 添加 Windows 标准库测试、只读原生启动检查和单独授权触发的安装包构建流程。未提供未经硬件验证的兼容性承诺。
- 提供 x64 安装 EXE、便携 ZIP 和 SHA-256；Windows 云端 41 项测试、打包托盘启动及安装／卸载验证通过。依赖哈希锁定，并随包附上 Python、Tcl/Tk 与相关第三方许可证。未签名，QDC507 Windows 实机测试尚未执行。
