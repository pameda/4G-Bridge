# TODO

## 用户新增：Wi-Fi 故障自动接管（待配置确认，尚未启用）

- [ ] 确认流量上限 GB 与周期（月累计／单次接管）。
- [ ] 确认触顶后手动恢复规则（追加额度／单次忽略上限）。
- [ ] 可选自动接管：Wi-Fi 断开或经受限探测确认不可上网时启用 4G，Wi-Fi 恢复后关闭。
- [ ] 触顶持久化锁定，重连、重启和周期变化不得绕过人工解锁；保留 VPN、DNS 与无关网络服务。
- [ ] 明确本机流量与运营商账单差异、采样延迟和可能超量；在用户批准额度前不进行实机自动开流量测试。

## 0.1.2 界面优化

- [x] 菜单栏原生快速面板和独立连接总览。
- [x] 真实速度趋势、本次／今日／本月流量，明确非套餐余额。
- [x] 浅色、深色和跟随系统外观。
- [x] 五页布局、缺设备和过渡状态、敏感操作确认、真实打包界面测试。
- [ ] 运营商套餐余量查询需单独设计发送确认与解析；当前不发送查询短信。
- [ ] 电话、固件刷写、应用级流量分析仍在 V1 范围之外。

## V1 硬件验收

- [x] 接入真实 QDC507 后记录只读 `ATI/CPIN/COPS/CEREG/QNWINFO/CSQ` 结果。
- [ ] 验证 `2CA3:4006` 与 `2C7C:0125` 两种 personality 的 endpoint 重新枚举。
- [ ] 验证模块热插拔、睡眠/唤醒和 ECM 接口重编号。
- [ ] 在用户明确确认后验证数据 ON/OFF、Wi-Fi/VPN 共存与退出恢复 OFF。
- [ ] 在用户明确确认目标后发送一次测试 iMessage。
- [ ] 使用专用测试短信验证成功转发后 `AT+CMGD`，并核对无重复。

## 发布

- [ ] 配置 Apple Developer ID 后替换 ad-hoc 签名并完成公证。
- [x] 完成 GitHub 私有仓库设备登录与首次 push。
- [x] 按用户要求将 GitHub 仓库设为公开，保留第一方许可证待定。
- [x] 用真实 tag `v0.1.0` 验证 GitHub Actions release。

## V2 预留

- [ ] 经用户单独授权后设计 iMessage → QDC507 SMS 出站协议。
- [ ] 不通过 `chat.db` 或 Full Disk Access 实现入站解析。
